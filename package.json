{
  "name": "tantyastorm-portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc -b && vite build && npm run routes:generate && npm run routes:check",
    "routes:generate": "node scripts/generate-static-routes.mjs",
    "routes:check": "node scripts/check-sitemap-routes.mjs",
    "preview": "vite preview --host 0.0.0.0",
    "format": "prettier --write ."
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.7",
    "typescript": "^5.7.2",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/react": "^19.0.2",
    "@types/react-dom": "^19.0.2",
    "prettier": "^3.4.2"
  }
}
